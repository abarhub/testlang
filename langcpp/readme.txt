Build du programme en C++ avec GCC

Pour builder, il faut faire ça :
docker build -t sort-test-cpp-app .

Pour executer, il faut faire :
docker run -it --rm --name sort-test-cpp-app-run sort-test-cpp-app
