Build du programme en Python 3.9

Pour builder, il faut faire ça :
docker build -t sort-test-python-app .

Pour executer, il faut faire :
docker run -it --rm --name sort-test-python-app-run sort-test-python-app
